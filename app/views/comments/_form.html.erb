<div class="comment_form">
<%= form_for comment, url: polymorphic_path([commentable, comment]), remote: true  do |f| %>
<%= f.text_area :content, placeholder: "Write a comment..." %>
<%= f.submit "Comment", class: "btn btn-primary accept" %>
<% end %>
</div>