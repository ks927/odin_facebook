<nav class="nav navbar inverse">
    <ul class="header_right">
      <% if user_signed_in? %>
        <li><%= link_to current_user.first, user_path(current_user) %></li>
        <li><%= link_to "Home", root_url %></li>
        <li><%= link_to "Notifications", '#' %></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu dropdown-menu-right">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Update Profile", user_registration_path(current_user) %></li>
              <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
            </ul>
        </li>
      <% else %>
        <li><%= link_to "Sign In", destroy_user_session_path %> or <%= link_to "Sign Up", new_user_registration_path %>
        <% end %></li>
      </ul>
</nav>