<div id="like_form_for_<%= @post.id %>">
<% if current_user.liked_posts.include?(post) %>
        <%= render 'likes/unlike' %>
      <% else %>
        <%= render 'likes/like' %>
      <% end %>
    <span id="likes"><%= pluralize(post.likes.count, "like") %></span>
      
</div>