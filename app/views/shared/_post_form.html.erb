<%= form_for(@post) do |f| %>
 
  <div class="field">
    <%= f.text_area :content, placeholder: "What's on your mind, #{current_user.first}?", id: "post_content" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary accept" %>
  
<% end %>